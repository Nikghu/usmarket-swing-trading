# Software Requirements Document — GUI Module (GUI)

**Document ID:** SRD-GUI
**Version:** 2.6.0
**Traces To:** FO-GUI v2.4.0
**Status:** Draft
**Last Updated:** 2026-05-13
> v2.6.0: Section 11 added — SRD-GUI-012.001–012 for Persistent IBKR Session (FO-GUI-012).
> v2.5.0: SRD-GUI-004.006–008 added — Filtered Stocks pane, FilteredStockEntry model, AppService cross-tool bridge (FO-GUI-004).
> v2.4.0: Section 10 added — SRD-GUI-011.001–004 for Candle Chart Viewer (FO-GUI-011).
> v2.3.0: SRD-GUI-006.006 updated — Universe tab candle status columns + Sync Candles button.
**Project:** US Swing Trading System

---

## Section 0: Admin Terminal Architecture

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-GUI-000.001 | FO-GUI-000 | Must | Single-admin multi-user terminal. `AppService.set_viewing_uid(uid: int \| None)` sets active scope. `None` = all-users aggregate; `int` = specific user. | Admin scope change | `viewing_changed` signal emitted | All panels connect to `viewing_changed` and refresh | Implemented |
| SRD-GUI-000.002 | FO-GUI-000 | Must | `_TitleBar` contains a `QComboBox` labelled "Scope:" populated with "🌐 All Users" and one entry per user (🔴 live / 🔵 paper). Selection calls `AppService.set_viewing_uid()`. | User interaction | Scope changed, all panels refresh | Combo syncs with dashboard scope pills | Implemented |
| SRD-GUI-000.003 | FO-GUI-000 | Must | `_AdminContextBar` is a 28px strip below the accent line. All-Users scope: user count, total equity, combined P&L, open position count. Single-user scope: username, equity, day P&L, open positions, risk %, mode, IBKR client ID. | `viewing_changed`, `positions_updated`, `account_updated` | Rendered context strip | Updates within 1 s of signal | Implemented |
| SRD-GUI-000.004 | FO-GUI-000 | Must | A `🔐 ADMIN` badge (yellow) appears in `_TitleBar` to the left of the "Scope:" label, right of the nav tabs stretch. | App launch | Badge rendered | Non-interactive; always visible | Implemented |
| SRD-GUI-000.005 | FO-GUI-000 | Must | All position mutations carry the `user_id` of the position row, not the viewing scope. | Admin action | Mutation targets correct user | Correct when admin views All-Users scope | Implemented |

---

## Section 1: Requirements for FO-GUI-001 — Main Window & System Status Bar

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-GUI-001.001 | FO-GUI-001 | Must | `MainWindow(QMainWindow)` is frameless. Layout (top→bottom): `_TitleBar` (40px) → HLine accent → `_AdminContextBar` (28px) → `QStackedWidget`. Nav: 4 tabs (Dashboard, Screener, Execution, Settings) in `_TitleBar`. | App launch | Main window rendered | `Qt.WindowType.FramelessWindowHint`; `AppService` is the single DI object | Implemented |
| SRD-GUI-001.002 | FO-GUI-001 | Must | `QStatusBar` (left, permanent): Internet status pill `"● Internet: Online/Offline"`, separator, `"P&L: +$X,XXX.XX"` (green/red), separator, `"Positions: N"`. Right permanent: `"⬤ NYSE"` and `"⬤ NASDAQ"` pills coloured by session state (open=green, pre_market=orange, after_hours=yellow, closed=muted). Updates on `account_updated`, `positions_updated`, `viewing_changed`, `internet_status_changed`, `market_status_updated`. | Signal events | Updated status bar | No scope label or connection-state label in status bar | Implemented |
| SRD-GUI-001.003 | FO-GUI-001 | Must | `MainWindow` saves and restores window geometry via `QSettings("USSwing", "MainWindow")`. Default size on first launch: **1180 × 740 px**, centred on primary screen. `windowState` is not saved. | App start/close | Persisted geometry | No separate `restore_geometry()` method; restore is inline in `__init__` | Implemented |
| SRD-GUI-001.004 | FO-GUI-001 | Must | Scope `QComboBox` in `_TitleBar` calls `AppService.set_viewing_uid()` on selection change; all panels react via `viewing_changed`. | User selection | Scope changed | Combo and dashboard scope pills stay in sync | Implemented |

---

## Section 2: Requirements for FO-GUI-002 — Dashboard Panel

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-GUI-002.001 | FO-GUI-002 | Must | `DashboardPanel` has a `QTableView` backed by `PositionTableModel`. Columns: Symbol, Qty, Avg Entry, Current, P&L ($), P&L %, Stop, Target, State. In All-Users scope, a prepended "User" column is added via `PositionTableModel.set_show_user(True, labels)`. | `viewing_changed`, `positions_updated` | Rendered table | `set_show_user(False)` in single-user scope | Implemented |
| SRD-GUI-002.002 | FO-GUI-002 | Must | A scope pill strip (32px) sits in the dashboard. Pills: `🌐 All` + one per user (🔴/🔵 by mode). Click calls `AppService.set_viewing_uid()`. Pills auto-sync on `viewing_changed`. | User click / `viewing_changed` | Scope changed; pill highlight updated | Pills use `setAutoExclusive(True)` | Implemented |
| SRD-GUI-002.003 | FO-GUI-002 | Must | Unrealised P&L column uses `BackgroundRole` (green/red tinted bg) and `ForegroundRole` (green/red text). | P&L value | Coloured cell | Implemented in `PositionTableModel._background/_foreground` | Implemented |
| SRD-GUI-002.004 | FO-GUI-002 | Must | KPI cards (P&L, equity, positions, capital) reflect current scope's data. Updates on `account_updated` and `viewing_changed`. | Signal events | Refreshed cards | Same refresh path for all scopes | Implemented |
| SRD-GUI-002.005 | FO-GUI-002 | Must | Trade history `QTableView` backed by `TradeHistoryModel`. Columns: Date & Time, Symbol, Side, Qty, Entry, Exit, P&L, Strategy, Mode. Optional "User" prefix column in All-Users scope. Sorted descending by entry time. | `viewing_changed` | Filtered history table | Sorted via `TradeHistoryModel.refresh()` | Implemented |
| SRD-GUI-002.006 | FO-GUI-002 | Must | "Manage Selected" action opens `_ExitPositionDialog` (frameless dialog, 580×540 min). Dialog has 4 tabs: Market Exit, Limit Exit, Scale Out, Stop/Trail. Admin attribution banner shown when acting on another user's position. | User interaction | Exit/update action on position | `user_id` taken from position row, not viewing scope | Implemented |

---

## Section 3: Requirements for FO-GUI-003 — Screener Panel

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-GUI-003.001 | FO-GUI-003 | Must | `ScreenerPanel` displays filter chips loaded from `AppService.get_screener_filters()`. Each chip has a `QCheckBox` (enable/disable) with an ON/OFF badge. Enabled chips expand to show `QDoubleSpinBox` parameter fields. | Panel init | Rendered filter chips | Filter state persistence to user settings not yet implemented | Implemented |
| SRD-GUI-003.002 | FO-GUI-003 | Must | Each filter's parameter spinboxes use `QDoubleSpinBox` with range 0–10,000. Spinboxes are disabled when the parent filter chip is unchecked. | User interaction | Updated parameter values | No per-filter custom range validation | Implemented |
| SRD-GUI-003.003 | FO-GUI-003 | Must | "▶ Run Screener" button triggers `_ScreenerWorker(QThread)`. Button disabled and text changes to "Running…" during run; indeterminate `QProgressBar` (4px height) visible. Re-enabled on completion. | Button click | Screener results emitted | Worker runs `AppService.get_screener_results()` with 1.4 s simulated delay | Implemented |
| SRD-GUI-003.004 | FO-GUI-003 | Must | Results `QTableView` columns: Symbol, Name, Sector, Score, RSI, Volume, Trend, Breakout, Pullback. Score column has tinted background (green ≥80, yellow ≥60, red <60). Filter columns show ✔/✘. Sortable (default: Score descending). Single "＋ Add to Watchlist" button in toolbar, enabled when a row is selected. | Screener results | Table populated | No per-row buttons; selection enables the toolbar button | Implemented |
| SRD-GUI-003.005 | FO-GUI-003 | Should | "Add to Watchlist" emits `watchlist_add_requested(symbol)` signal; `MainWindow` forwards this to `DashboardPanel.on_watchlist_add()`. | Button click + selection | Signal emitted; result label updated | Watchlist capacity enforcement not implemented | Implemented |

---

## Section 4: Requirements for FO-GUI-004 — Trade Execution Panel

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-GUI-004.001 | FO-GUI-004 | Must | `ExecutionPanel` header has a `🔐 ADMIN` badge and "Execute for:" `QComboBox` populated with "🌐 All / Broadcast" and one entry per user. | Panel init | Rendered header | Combo auto-syncs with `viewing_changed` | Implemented |
| SRD-GUI-004.002 | FO-GUI-004 | Must | `_on_scope_changed()` connects to `AppService.viewing_changed` and updates the Execute-for combo index to match the global scope. | `viewing_changed` | Combo index updated | Prevents scope desync | Implemented |
| SRD-GUI-004.003 | FO-GUI-004 | Must | On "Execute" click, `QMessageBox` shows symbol, qty, entry price, mode, and `"🔐 Admin · Executing for: <user>"` attribution in yellow. Execute proceeds only on Yes. | Execute button click | Confirmation dialog; `AppService.execute_signal()` called on Yes | Row disabled after submission; button text → "✔ Submitted" | Implemented |
| SRD-GUI-004.004 | FO-GUI-004 | Must | One `_SignalRow` widget per pending signal: symbol, strategy, entry/stop/target/R:R labels, recommended-qty `QSpinBox`, "Execute {side}" button. | `AppService.get_pending_signals()` | Rendered signal rows | Rows are static (loaded at init); no dynamic signal arrival yet | Implemented |
| SRD-GUI-004.005 | FO-GUI-004 | Must | When circuit breaker is active: all `_SignalRow` execute buttons disabled, "(Circuit Breaker Active)" banner visible. Deactivated on `on_circuit_breaker(False)`. | `on_circuit_breaker(True/False)` | Banner shown/hidden; buttons enabled/disabled | No exit buttons in this panel; circuit breaker only gates new entries | Implemented |
| SRD-GUI-004.006 | FO-GUI-004 | Must | `_FilteredStocksPane(QWidget)` is the left pane of a `QSplitter` in `ExecutionPanel` (initial sizes [260, 900], neither side collapsible). Layout (top→bottom): header row ("FILTERED STOCKS" muted label + count `QLabel`), date-filter row ("Date" label + `QComboBox` width 120px), `QTableView` backed by `_FilteredStocksModel` (columns: Symbol, Score, Run, Style, Screener; sortable; default sort Score descending; alternating rows; no grid; no row header), empty-state `QLabel` ("No screener results yet. Run a preset in the Screener tab.") shown when the selected date returns zero entries. Min width 180px. | `svc.screener_results_updated` signal | Pane re-renders with filtered entries | `QComboBox.blockSignals(True/False)` guards date combo population to suppress spurious filter calls | Implemented |
| SRD-GUI-004.007 | FO-GUI-004 | Must | `FilteredStockEntry` `@dataclass` in `data/models.py`: fields `symbol: str`, `score: float`, `trading_styles: list[str]`, `assigned_users: list[str]`, `screener_name: str`, `run_type: str` ("manual"\|"scheduled"), `date: str` (YYYY-MM-DD). `_FilteredStocksModel` uses `ForegroundRole` for Score column: green (`C.GREEN`) when score ≥ 0.70, yellow (`C.YELLOW`) when ≥ 0.40, red (`C.RED`) otherwise. Run column displays "Auto" for `run_type == "scheduled"` (teal, `C.TEAL`) and "Manual" otherwise (blue, `C.BLUE`). | Screener run result | Typed model instance; coloured table cells | Dataclass lives in shared `data/models.py`, not in GUI layer | Implemented |
| SRD-GUI-004.008 | FO-GUI-004 | Must | AppService cross-tool bridge for screener→execution data flow. `screener_results_updated = pyqtSignal(list)` (list[FilteredStockEntry]). `get_latest_screener_results() -> list[FilteredStockEntry]`: scans `~/.usswing/screener_results/preset_*/` dirs, loads the most recent date file per preset via `ScreenerResultsStorage`, builds one `FilteredStockEntry` per passing symbol; symbols appearing in multiple presets are deduped keeping the highest score entry. `notify_screener_results_updated()`: calls `get_latest_screener_results()` and emits `screener_results_updated`. `ScreenerPanel` calls `notify_screener_results_updated()` after every successful preset run. `_FilteredStocksPane` connects to `screener_results_updated` on init and calls `get_latest_screener_results()` at startup to populate from persisted results without requiring a fresh run. | Screener run completion or panel init | Updated `list[FilteredStockEntry]` emitted | `ScreenerResultsStorage` and `PresetManager` are lazy-imported inside the method to avoid circular imports | Implemented |

---

## Section 5: Requirements for FO-GUI-005 — Position Monitor Panel

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-GUI-005.001 | FO-GUI-005 | Must | `PositionMonitorPanel` has a `QTableView` backed by `PositionTableModel`. Columns: Symbol, Qty, Avg Entry, Current, P&L ($), P&L %, Stop, Target, State. Positions loaded from `AppService.get_positions()`. Refreshes on `positions_updated`. | `positions_updated` | Rendered position table | No "User" column in this panel; single-user view only | Implemented |
| SRD-GUI-005.002 | FO-GUI-005 | Must | Capital indicator (`_CapitalRow`): label shows `"$available of $equity"` where `available = equity − open_position_value`. Colour: green if available > 0, red otherwise. `QProgressBar` shows utilisation % (`open_val / equity × 100`), styled alert (property="alert") when ≥ 90%. | `account_updated` | Colour-coded label + progress bar | Formula uses raw equity and open_position_value from `AccountState` | Implemented |
| SRD-GUI-005.003 | FO-GUI-005 | Must | "CAN ENTER" / "CANNOT ENTER" badge. Logic: `can = available > 0 and util_pct < max_capital_pct`. Badge uses `objectName` `can_enter_yes` / `can_enter_no` for QSS styling. | `AccountState`, active user's `max_allocation_pct` | Yes/No badge | Inline logic; no external `RiskManager` call | Implemented |
| SRD-GUI-005.004 | FO-GUI-005 | Should | State column colour coding via `PositionTableModel`: NEW=grey, PARTIAL_ENTRY=yellow, OPEN=green, PARTIAL_EXIT=orange, CLOSED=muted. Applied via `BackgroundRole` and `ForegroundRole`. | `position.state` | Coloured cell | Theme colours from `C.STATE_*` constants | Implemented |

---

## Section 6: Requirements for FO-GUI-006 — Settings Panel

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-GUI-006.001 | FO-GUI-006 | Must | `SettingsPanel` is a `QTabWidget` with 5 tabs in order: Users, Strategies, System, Universe, Database. | Panel init | Rendered sub-tabs | No standalone "Risk & Position" tab; risk settings are in the User dialog. Screeners tab removed (v2 preset config lives in the Screener panel). | Implemented |
| SRD-GUI-006.002 | FO-GUI-006 | Must | Users tab (`_UsersTab`): `QTableWidget` (11 columns: ID, Username, Display Name, IBKR ID, Mode, Risk %, Max Capital %, Max Position, Max Daily Loss, Default Order, Order Confirm). New/Edit opens `_UserDialog` which also edits risk settings. Delete confirms via `QMessageBox`; blocked if `AppService.delete_user()` returns an error string. | Button click | User CRUD via `AppService` | `users_changed` signal triggers table refresh | Implemented |
| SRD-GUI-006.003 | FO-GUI-006 | Must | Strategies tab (`_StrategiesTab`): `QGroupBox` per strategy (BREAKOUT, PULLBACK). Each has an "Enabled" `QCheckBox` and `QDoubleSpinBox` fields per parameter. No save action implemented yet. | Panel init | Rendered strategy config widgets | Parameters not persisted to `AppService` in current implementation | Implemented |
| SRD-GUI-006.004 | FO-GUI-006 | Must | ~~Screeners tab (`_ScreenersTab`)~~ **Removed.** v1 flat `ScreenerFilter` model superseded by v2 JSON preset architecture. All screener config is managed in the Screener panel via `_PresetBuilderDialog`. `ScreenerFilter` dataclass and `_DEFAULT_FILTERS` also deleted. | — | — | Removed in v2 | Verified |
| SRD-GUI-006.005 | FO-GUI-006 | Must | System tab (`_SystemTab`): fields for IBKR host (`QLineEdit`), port (`QSpinBox` 1–65535), log level (`QComboBox`: DEBUG/INFO/WARNING/ERROR), scheduler enabled (`QCheckBox`), market open time (`QLineEdit`), market close time (`QLineEdit`). "💾 Save Settings" persists via `AppService.save_system_config()`. No confirmation dialog. | User interaction | `SystemConfig` saved | No restart required; IBKR client ID is per-user (not here) | Implemented |
| SRD-GUI-006.006 | FO-GUI-006 | Should | Universe tab (`_UniverseTab`): `QWebEngineView` renders an HTML table with embedded JS for sorting and column resize/reorder. Columns: #, Symbol, IBKR, Sector, Mkt Cap, Name, **First Bar**, **Last Bar**, **Status**. `QLineEdit` search bar filters by symbol/name/sector. "🔄 Refresh" button calls `AppService.refresh_sp500_universe()`. "🔄 Sync Candles" button calls `AppService.sync_candle_data()`; disabled while sync is running. Table reloads on `sp500_updated` and `candle_sync_updated` signals. Status column: `● up_to_date` (green), `● stale` (orange), `● missing` (red). Header shows constituent count + candle readiness summary (e.g., "482 / 503 up to date"). | Panel init; refresh/sync buttons | HTML universe table | Read-only; no row selection or edit; Mkt Cap formatted as $T/$B/$M | Draft |
| SRD-GUI-006.007 | FO-GUI-006 | Must | Users table has 12 columns (add "Admin" after "Mode"). Admin column shows "✓" for users where `is_admin=True`, blank otherwise. Column is read-only; admin toggle is only via the Edit dialog. | `users_changed` signal | Table refreshed with Admin column | Admin status read from `UserProfile.is_admin` | Draft |
| SRD-GUI-006.008 | FO-GUI-006 | Must | `_UserDialog` (New / Edit) has an "Administrator" `QCheckBox`. When editing the only admin user, the checkbox is disabled (greyed) to prevent self-demotion. On save, `is_admin` is included in the `UserProfile` passed to `AppService.add_user()` / `AppService.update_user()`. | User dialog open | `is_admin` flag saved | Disabled only when editing the last admin; other users' admin checkboxes are freely togglable | Draft |
| SRD-GUI-006.009 | FO-GUI-006 | Must | Delete User: if `AppService.delete_user()` raises `LastAdminError` (the selected user is the only admin), show `QMessageBox.critical` with message "Cannot delete the only administrator. Assign admin role to another user first." Delete does not proceed. | Delete button click | Error dialog shown; no user deleted | Extends existing blocked-delete logic in SRD-GUI-006.002 | Draft |
| SRD-GUI-006.010 | FO-GUI-006 | Must | System tab adds a "System IBKR Client ID" `QSpinBox` (range 0–99999, default 0) placed adjacent to the IBKR host/port row. Bound to `SystemConfig.ibkr_system_client_id`. Tooltip: "Used for market data — must differ from all user client IDs." Saved on "💾 Save Settings". | User interaction | `SystemConfig.ibkr_system_client_id` saved | No clash validation in the GUI; clash is caught at INF startup (SRD-INF-001.006) | Draft |
| SRD-GUI-006.011 | FO-GUI-006 | Must | `SettingsPanel` has a 6th tab "Database" (`_DatabaseTab`). **Status card** (always visible): colored badge (`EMPTY`=red, `PARTIAL`=orange, `CURRENT`=green), "Data through: YYYY-MM-DD", "N / M symbols · Daily + Weekly", total candle count, last trading day. "↻ Refresh Status" button triggers background `_CandleDbStatusWorker`. Status is computed by querying `~/.usswing/candles.db` (`price_1d`, `price_1w` tables); compared against last NYSE trading day from `market_calendar.py`. **Build section**: (a) `EMPTY` — shows `QDateEdit` start-date picker (default today − 2 years) + "⬇ Build Full Database" button; (b) `PARTIAL` — "🔄 Fill Delta" button (auto-starts from last candle date); (c) `CURRENT` — disabled "✔ Database Current" button. **Progress section** (visible only while worker running): determinate `QProgressBar` 0–100%, status label "Downloading AAPL… (45 / 503)", "✕ Cancel" button. If a prior interrupted download checkpoint exists, the build section shows "▶ Resume Download" instead of the normal build button. AppService signals: `candle_db_status_changed(CandleDbInfo)`, `candle_download_progress(str, int, int)`, `candle_download_finished(int, int)`, `candle_download_failed(str)`, `candle_download_paused(str)`. | Panel init; button clicks | DB status displayed; download initiated; progress shown | DB path `~/.usswing/candles.db`; uses existing `price_1d`/`price_1w` schema; IBKR as download source (requires CONNECTED status — see SRD-GUI-006.012); checkpoint at `~/.usswing/candle_download_checkpoint.json` | Draft |
| SRD-GUI-006.012 | FO-GUI-006 | Must | **IBKR connection gate.** `AppService.start_candle_download()` checks `connection_status is ConnectionStatus.CONNECTED` before starting the worker. If not connected: emits `candle_download_failed("IBKR_NOT_CONNECTED")` and logs `WARNING "[CandleDB] IBKR Gateway not connected — connect the feed first."`. `_DatabaseTab._on_build_clicked()` shows `QMessageBox.warning` (title "IBKR Not Connected", body "Connect the IBKR feed via the title-bar button before downloading candle data.") and returns without starting download. The download worker itself creates a dedicated `ib_insync.IB()` connection using `SystemConfig.ibkr_host`, `ibkr_port`, and `ibkr_system_client_id`; connection failure emits `failed("IBKR_NOT_CONNECTED: <reason>")`. | Build/Resume button click | Guard check; warning dialog if not connected | Does not guard `refresh_candle_db_status()` — status check is always allowed | Draft |
| SRD-GUI-006.013 | FO-GUI-006 | Must | **Download checkpoint & resume.** `_CandleDownloadWorker` writes `~/.usswing/candle_download_checkpoint.json` after each symbol completes. Schema: `{version:1, start_date, end_date, mode, symbols_done:[…], symbols_todo:[…], created_at}`. On clean finish the checkpoint file is deleted. On stop/cancel/error it persists. On next `start_candle_download()` call: if checkpoint exists with matching `mode` and `start_date`, worker enters resume mode — skips `symbols_done` and re-verifies the last 5 completed symbols (queries `MAX(datetime)` per symbol in `price_1d`; if any symbol's last date is more than 5 calendar days before `end_date`, it is moved back to `symbols_todo`). `_DatabaseTab` checks for checkpoint existence on init and on status load: if a checkpoint exists, the build button is replaced with "▶ Resume Download". `AppService.has_candle_checkpoint() → bool` exposes this state. | Download start/stop; app restart | Interrupted download resumes; at-risk symbols re-downloaded | Checkpoint is keyed by mode + start_date; a full-mode checkpoint does not resume a delta-mode call | Draft |
| SRD-GUI-006.014 | FO-GUI-006 | Must | **IBKR disconnect mid-download.** If `ib_insync` raises a connection-loss exception during download, the worker emits `failed("IBKR_DISCONNECTED")` after completing the current symbol's DB write (no partial writes). The checkpoint is preserved. `AppService._on_candle_failed()` routes reason `"IBKR_DISCONNECTED"` to the new `candle_download_paused(str)` signal (reason string passed through). `_DatabaseTab._on_paused()` sets progress label to "⏸ Paused — IBKR disconnected", hides the cancel button, shows "▶ Resume" build button, and calls `_set_downloading(False)` so status can be refreshed. When the user reconnects and clicks "▶ Resume", normal `start_candle_download()` logic applies (checkpoint auto-detected). | IBKR connection drop during download | Progress shows paused state; checkpoint preserved; resume possible after reconnect | Auto-resume on reconnect is not implemented — user must click Resume | Draft |
| SRD-GUI-006.015 | FO-GUI-010 | Should | **Per-symbol failure tracking.** `_CandleDownloadWorker` adds `symbol_failed = pyqtSignal(str, str)` (symbol, reason). The `except Exception` block in `_download_all` captures the exception message and emits `symbol_failed(symbol, str(exc) or "NO_DATA")` instead of silently passing. Empty-bars results (both `bars_1d` and `bars_1w` are empty) also emit `symbol_failed(symbol, "NO_DATA")`. `AppService` adds `candle_symbol_failed = pyqtSignal(str, str)` and `candle_download_failures = pyqtSignal(object)` (emits `list[str]`). `AppService` accumulates failures in `_current_failed: list[str]`, reset to `[]` at the start of each `start_candle_download()` call. `_on_candle_finished` saves `_current_failed` to `~/.usswing/candle_failed_symbols.json` (JSON: `{version:1, symbols:[…], created_at:…}`) when non-empty, then emits `candle_download_failures(_current_failed)`. If `_current_failed` is empty the file is deleted. `AppService` exposes `get_failed_symbols() -> list[str]` (loads from file; returns `[]` if missing) and `has_failed_symbols() -> bool`. `start_candle_download()` gains an optional `symbols: list[str] | None = None` parameter; when provided it bypasses the universe lookup and checkpoint logic and downloads only those symbols. Each per-symbol failure is also logged at WARNING: `[CandleDB] Symbol {symbol} failed — {reason}`. | Download run | `symbol_failed` emitted per failure; failures file written; `candle_download_failures` emitted at end | Empty-bars check: both lists empty → NO_DATA; partial (one empty) is still written | Approved |
| SRD-GUI-006.016 | FO-GUI-010 | Should | **Fix Discrepancies UI.** `_DatabaseTab` adds a "Discrepancies" `QGroupBox` (initially hidden) below the build group. Contents: warning label `"⚠  N symbols have no data:"`, a read-only `QLabel` listing symbols (comma-separated, truncated to 80 chars with "…" if longer), and a `"🔧  Fix Discrepancies"` `QPushButton`. Signal wiring: `svc.candle_symbol_failed` → `_on_symbol_failed(symbol, reason)` increments a live fail counter shown in the progress label suffix `"  (N failed)"`. `svc.candle_download_failures` → `_on_failures_ready(symbols)` shows/hides the discrepancies group and populates it. `_DatabaseTab` calls `_load_persisted_failures()` on init (after status refresh) to show the group immediately if a failures file exists from a prior session. Clicking "Fix Discrepancies" checks IBKR connection (same gate as SRD-GUI-006.012), then calls `svc.start_candle_download(start_date=today, mode="fix", symbols=failed_list)`. On `candle_download_failures([])` (empty list after fix run), the discrepancy group is hidden and the failures file deleted via `svc.clear_failed_symbols()`. | Download finish; app init | Discrepancy group shown/hidden; Fix button triggers targeted re-download | Group hidden during active download; re-shown after completion if failures remain | Approved |

---

## Section 7: Requirements for FO-GUI-007 — Log Viewer Panel

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-GUI-007.001 | FO-GUI-007 | Should | `LogViewerPanel` displays log entries in a read-only `QTextEdit` (monospace, no line wrap). Entries arrive via `AppService.log_message(level, message)` signal. Buffer: `list[tuple[str, str]]` capped at 10,000 entries (FIFO eviction). | `AppService.log_message` signal | Coloured HTML log entries in `QTextEdit` | Not wired to Python `logging` module; driven by `AppService` signals only | Implemented |
| SRD-GUI-007.002 | FO-GUI-007 | Should | Filter row: level `QComboBox` (ALL/DEBUG/INFO/WARNING/ERROR), keyword `QLineEdit` ("Filter by keyword…"). Filters apply without clearing buffer (`_reapply_filter` re-renders from buffer). Also has: "⏸ Pause / ▶ Resume" toggle button, "🗑 Clear" button, "Auto-scroll" `QCheckBox`. | User interaction | Filtered log view re-rendered | No separate module or symbol filter fields | Implemented |
| SRD-GUI-007.003 | FO-GUI-007 | Should | ERROR and CRITICAL entries: red background in log view. Emits `error_occurred` signal. Status bar flash not implemented. | ERROR/CRITICAL log entry | Red-background HTML entry; `error_occurred` emitted | No wiring from `error_occurred` to MainWindow status bar in current implementation | Implemented |
| SRD-GUI-007.004 | FO-GUI-007 | Should | Log buffer capped at `MAX_LINES = 10_000`. When exceeded, buffer sliced to last 10,000. No disk spill. | Buffer full + new entry | Oldest entries removed | FIFO via `self._buffer = self._buffer[-self.MAX_LINES:]` | Implemented |

---

## Section 8: Requirements for FO-GUI-008 — Feed Connection Management

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-GUI-008.001 | FO-GUI-008 | Must | Feed toggle button (140×26px) in `_TitleBar`, placed after Scope combo. Three states: "Connect Feed" (neutral, enabled) when DISCONNECTED; "⟳ Connecting…" (yellow, disabled) when RECONNECTING; "🟢 Connected" (green border, enabled) when CONNECTED. Driven by `AppService.feed_status_changed(str)`. | Button click / `feed_status_changed` | `AppService.connect_feed()` or `disconnect_feed()` called | Disconnect requires confirmation (SRD-GUI-008.002) | Implemented |
| SRD-GUI-008.002 | FO-GUI-008 | Must | Clicking "🟢 Connected" shows `QMessageBox.question` (Yes/No). `disconnect_feed()` called only on Yes. RECONNECTING state ignores click. | Button click | `disconnect_feed()` or no-op | `QMessageBox.StandardButton.Yes` guard | Implemented |
| SRD-GUI-008.003 | FO-GUI-008 | Must | `AppService.connection_status` is a typed `ConnectionStatus` enum property. `feed_status_changed(str)` broadcasts `ConnectionStatus.value` for GUI slots. | AppService state | `ConnectionStatus` enum instance | Enum identity comparison preferred in non-GUI code | Implemented |
| SRD-GUI-008.004 | FO-GUI-008 | Must | Paper mode only. `AppService.execute_signal()` logs `[PAPER]` prefix. `_DEFAULT_PAPER_EQUITY = 100_000.0`. No live execution path. | Signal execution | Paper log entry | No live mode toggle in UI or code | Implemented |

---

## Section 9: Requirements for FO-GUI-009 — Market Watch

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-GUI-009.001 | FO-GUI-009 | Must | Market Watch is rendered inline in `_AdminContextBar` (MainWindow, not Dashboard). Shows up to 3 market cells: display name, LTP (`$X,XXX.XX`), daily change % (green/red). Separated from scope items by a `"│"` divider. Default symbols: ^GSPC, ^IXIC, ^DJI. | `AppService.market_watch_updated` | Rendered market cells | Max 3 cells; placeholder "–" when data unavailable or disconnected | Implemented |
| SRD-GUI-009.002 | FO-GUI-009 | Must | `AppService` owns `_watch: list[MarketWatchItem]` and `market_watch_updated` signal. `_refresh_market_watch()` spawns `_MarketWatchWorker(QThread)` to fetch via `yfinance.fast_info`. Timer fires every 15 s while connected; fires immediately on connect. Timer stopped on disconnect. | Feed connect / QTimer | `MarketWatchItem.ltp`, `change_pct` updated | Non-blocking; runs in `QThread` | Implemented |
| SRD-GUI-009.003 | FO-GUI-009 | Should | Edit dialog for Market Watch symbols: `QDialog` with 3 rows of (symbol, display_name) `QLineEdit` fields. Saving calls `AppService.set_market_watch_symbols()`. Empty rows ignored. | Edit button click | Updated watch list + immediate refresh | Not yet implemented in `_AdminContextBar` | Implemented |
| SRD-GUI-009.004 | FO-GUI-009 | Should | Daily change % coloured green (positive) or red (negative) using `C.GREEN` / `C.RED`. LTP as `"$X,XXX.XX"`. All cells reset to "–" when disconnected. | `MarketWatchItem.change_pct` | Coloured `QLabel` | Applied in `_AdminContextBar._refresh_mw()` | Implemented |

---

## Section 10: Requirements for FO-GUI-011 — Candle Chart Viewer

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-GUI-011.001 | FO-GUI-011 | Should | `CandleChartPanel(QWidget)` in `chart_panel.py` — "📈 Chart" nav tab (index 3, before Settings). Toolbar (44px fixed height): "Symbol" editable `QComboBox` (width 120px, case-insensitive contains `QCompleter`, max 12 suggestions), "Timeframe" `QComboBox` (`["1d","1w"]`, width 70px), "Bars" `QSpinBox` (range 20–2000, default 500, step 50, width 70px), "Load Chart" `QPushButton`, stretch, status `QLabel`, "↺ Refresh List" `QPushButton`. Below toolbar: `QWebEngineView` (stretch=1) renders a self-contained HTML page. | `AppService`, panel show event | Rendered chart panel | `QWebEngineView` already in deps via `settings_panel.py` Universe tab | Implemented |
| SRD-GUI-011.002 | FO-GUI-011 | Should | **Symbol list population.** `_refresh_symbol_list()` calls `AppService.get_candle_symbols() -> list[str]` (queries `SELECT DISTINCT symbol FROM price_1d ORDER BY symbol` on `~/.usswing/candles.db`; returns `[]` if DB missing). Dropdown is repopulated; previously selected symbol is preserved if still present. `QCompleter` model updated with the new list. Status label shows `"N symbols in DB — select one and click Load Chart"` or `"No candle data in DB yet"`. `showEvent` triggers `_refresh_symbol_list()` every time the tab becomes visible. | Tab show / Refresh List click | Symbol dropdown + completer updated | `blockSignals(True/False)` guards combo repopulation to suppress spurious `currentIndexChanged` | Implemented |
| SRD-GUI-011.003 | FO-GUI-011 | Should | **Chart rendering.** `_load_chart(symbol, timeframe, limit)` calls `AppService.get_candles_for_symbol(symbol, timeframe, limit) -> list[dict]`. Each dict has keys: `time` (Unix timestamp, seconds), `open`, `high`, `low`, `close`, `volume`. `_build_html()` produces a self-contained HTML page inlining the TradingView Lightweight Charts v5 JS bundle from `gui/resources/lightweight-charts.standalone.production.js`; CDN fallback used when bundle is missing. Page contains: (1) a candlestick `CandlestickSeries` (up=#26a69a, down=#ef5350); (2) an 80px `HistogramSeries` volume chart synced to the main time-scale; (3) a crosshair `subscribeCrosshairMove` handler that writes `date O H L C Vol` to the header bar; (4) a `ResizeObserver` that calls `applyOptions({width, height})` on both charts. Placeholder HTML is shown on init; "No data" HTML is shown when `get_candles_for_symbol` returns `[]`. | Load Chart click / Enter key | `QWebEngineView.setHtml()` called with rendered page | `setHtml(html, QUrl("about:blank"))` — no file:// URL needed; JS runs inline | Implemented |
| SRD-GUI-011.004 | FO-GUI-011 | Should | **Auto-reload triggers.** Changing the timeframe `QComboBox` or finishing editing the bars `QSpinBox` calls `_auto_reload_if_loaded()`, which calls `_on_load()` only when `_current_symbol` is non-empty (i.e., a chart was previously loaded). Pressing Enter in the symbol `QLineEdit` calls `_on_load()` directly. | Timeframe change / spinbox edit / Enter key | Chart reloaded for same symbol | No reload on combo change when no chart has been loaded yet | Implemented |

---

## Section 11: Requirements for FO-GUI-012 — Persistent IBKR Session

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-GUI-012.001 | FO-GUI-012 | Must | `IBKRSession(QObject)` in `src/us_swing/gui/ibkr_session.py` owns one `ib_insync.IB` instance and the dedicated asyncio event loop that drives it. Public Qt signals: `account_ready(AccountState, list[OpenPosition])`, `quotes_updated(list[dict])` (combined Market Watch + Watchlist ticks), `connection_lost(str)`, `connection_restored()`. Public methods: `start(host, port, client_id) -> None`, `stop() -> None`, `set_market_watch_symbols(list[str]) -> None`, `set_watchlist_symbols(list[str]) -> None`. | `AppService` call | Session managed, signals emitted | Class lives in `gui/`; never imported by non-GUI modules | Approved |
| SRD-GUI-012.002 | FO-GUI-012 | Must | Session asyncio loop runs on a dedicated `QThread` (not the Qt main thread). `start()` launches the thread; `stop()` cancels all pending tasks, calls `ib.disconnect()`, joins the thread within 3 s. Inbound `ib_insync` events are marshalled to the Qt main thread via `pyqtSignal.emit()` — never via direct slot calls. | Thread lifecycle | Qt-thread-safe signal delivery | No `ib_insync` object touched from the Qt thread except via `QMetaObject.invokeMethod` or queued connections | Approved |
| SRD-GUI-012.003 | FO-GUI-012 | Must | `AppService.connect_feed()` after a successful `_ConnectWorker` probe instantiates `IBKRSession` and calls `start(host, port, ibkr_system_client_id)`. `disconnect_feed()` calls `IBKRSession.stop()` and releases the reference. `connect_feed()` must be idempotent — calling it twice without an intervening `disconnect_feed()` does not spawn a second session. | `connect_feed()` / `disconnect_feed()` | Session lifecycle tied to feed status | The pre-existing TCP probe via `_ConnectWorker` is preserved unchanged | Approved |
| SRD-GUI-012.004 | FO-GUI-012 | Must | On session start, `IBKRSession` calls `ib.reqAccountUpdates(True, account="")` and subscribes to `ib.accountValueEvent`, `ib.updatePortfolioEvent`, and `ib.accountSummaryEvent`. On any of these events firing, the session debounces (50 ms coalescing window) and emits `account_ready(AccountState, list[OpenPosition])` with the latest snapshot built from `ib.accountValues()` + `ib.portfolio()`. | IBKR push | `account_ready` signal | No 1.5 s `asyncio.sleep` priming — subscription is event-driven | Approved |
| SRD-GUI-012.005 | FO-GUI-012 | Must | Market-data subscriptions: for every symbol in `set_market_watch_symbols` ∪ `set_watchlist_symbols`, the session creates a `Stock(symbol, "SMART", "USD")` contract and calls `ib.reqMktData(contract, "", False, False)`. Tickers are tracked in `dict[str, Ticker]`. On `pendingTickersEvent`, the session builds a combined `list[dict]` (`symbol`, `ltp`, `change_pct`, `previous_close`, `source="ibkr"`) and emits `quotes_updated`. Symbols prefixed with `^` (indices) are excluded — they fall back through the existing yfinance path in `AppService`. | IBKR tick / symbol set change | `quotes_updated` signal | Coalescing window 250 ms to avoid Qt-thread thrash | Approved |
| SRD-GUI-012.006 | FO-GUI-012 | Must | Symbol-set mutation: `set_market_watch_symbols(new)` and `set_watchlist_symbols(new)` compute set differences against the prior subscription. For removed symbols, `ib.cancelMktData(contract)` is invoked and the ticker is dropped. For added symbols, `reqMktData` is invoked. Symbols present in either of the two sets remain subscribed — never cancelled and resubscribed unnecessarily. | `AppService.set_market_watch_symbols()` / `set_watchlist()` | Subscription delta applied | Pre-existing `AppService.set_market_watch_symbols` signature unchanged | Approved |
| SRD-GUI-012.007 | FO-GUI-012 | Must | Reconnect state machine: on `ib.disconnectedEvent` while the session is supposed to be running, the session emits `connection_lost(reason)` and enters a reconnect loop with exponential backoff (initial 2 s, max 30 s, jittered ±20 %). On reconnect success, it re-runs `reqAccountUpdates` and re-subscribes every tracked symbol, then emits `connection_restored()`. `AppService` bridges `connection_lost` to `feed_status_changed=RECONNECTING` and `connection_restored` to `feed_status_changed=CONNECTED` — no new `ConnectionStatus` enum value is added. | `ib.disconnectedEvent` | Resubscription completed | Reconnect attempts loop until either `stop()` is called or 10 consecutive failures, at which point session emits `connection_lost` final and AppService transitions to `DISCONNECTED` | Approved |
| SRD-GUI-012.008 | FO-GUI-012 | Must | `AppService._on_account_data_ready` (renamed `_on_session_account_ready`) is wired to `IBKRSession.account_ready`. It updates `_ibkr_acct` and `_ibkr_positions`, then emits the existing public `account_updated` and `positions_updated` signals unchanged. No panel-facing signal signature is modified. | `account_ready` | Existing public signals fired | Bridge slot lives on `AppService` instance | Approved |
| SRD-GUI-012.009 | FO-GUI-012 | Must | `AppService._on_session_quotes_updated(list[dict])` partitions quotes into Market Watch (symbols matching `_market_watch_symbols`) and Watchlist (symbols matching `_watchlist`). It updates `_watch` and `_wl_quotes` and emits the existing public `market_watch_updated` and `watchlist_updated` signals with unchanged signatures. Index symbols (`^GSPC`, `^IXIC`, `^DJI`) and any symbol the IBKR session reports `np.nan` for are filled in via the existing yfinance fallback (`_MarketWatchYfinanceWorker` — see SRD-GUI-012.012). | `quotes_updated` | Public quote signals fired | No new pyqtSignal added | Approved |
| SRD-GUI-012.010 | FO-GUI-012 | Must | yfinance fallback contract: when `AppService.connection_status` is `DISCONNECTED`, Market Watch and Watchlist quotes are fetched by a single-purpose yfinance worker (`_MarketWatchYfinanceWorker`) on a 30 s timer — preserving today's offline UX. When `CONNECTED`, this worker is **not** scheduled; the IBKR session is the sole quote source except for the index-symbol carve-out in SRD-GUI-012.009. | Connection status change | Active quote source switched | The IBKR session never opens a yfinance connection itself; isolation between paths is enforced at `AppService` | Approved |
| SRD-GUI-012.011 | FO-GUI-012 | Must | ClientId allocation: the persistent monitoring session uses exactly `system_cfg.ibkr_system_client_id`. The legacy `ibkr_system_client_id + 1`, `ibkr_mw_client_id`, and `ibkr_wl_client_id` allocations are removed from `AppService` and from any orphaned references in `system_store.py` / `SystemConfig`. `ibkr_intraday_client_id` (candle download) and `ibkr_live_client_id` (live-bar worker) are preserved unchanged. | App start | One monitoring clientId in use | If `ibkr_mw_client_id` / `ibkr_wl_client_id` fields are still persisted in `system_config.json`, they are silently ignored on load and not re-written | Approved |
| SRD-GUI-012.012 | FO-GUI-012 | Must | Cleanup deletions in `app_service.py`: the classes `_AccountDataWorker`, `_MarketWatchWorker`, and `_WatchlistQuoteWorker` are deleted in full; the `QTimer` fields `_acct_timer`, `_watch_timer`, `_wl_timer` and their `singleShot`/`start()`/`stop()` call sites are deleted; the methods `_refresh_account_data`, `_refresh_market_watch`, `_refresh_watchlist`, `_on_account_data_failed` are deleted; the instance fields `_acct_worker`, `_mw_worker`, `_wl_worker`, `_mw_log_on_next_fetch` are deleted; no commented-out blocks or "removed" placeholders remain. The yfinance fallback worker introduced in SRD-GUI-012.010 is the only non-IBKR quote path that remains. | Refactor | Legacy code gone | Verified by grep returning zero hits for each deleted identifier | Approved |
